# 🏗️ Архитектура ToDo Bot

## Обзор системы

Проект представляет собой микросервисную архитектуру для управления задачами через Telegram бота с Django backend.

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Telegram Bot  │◄──►│   Django API    │◄──►│   PostgreSQL    │
│   (aiogram)     │    │   (REST API)    │    │   (Database)    │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         │              ┌─────────────────┐              │
         └─────────────►│     Redis       │◄─────────────┘
                        │  (Cache/Queue)  │
                        └─────────────────┘
                                 │
                        ┌─────────────────┐
                        │     Celery      │
                        │  (Background)   │
                        └─────────────────┘
```

## Компоненты системы

### 1. Django Backend (`django_backend/`)
**Назначение**: Основной API сервер для управления данными

**Технологии**:
- Django 4.2.7
- Django REST Framework
- PostgreSQL
- Часовой пояс: America/Adak

**Ключевые особенности**:
- Кастомные Primary Key (хеши вместо UUID/автоинкрементов)
- REST API для задач и категорий
- Административный интерфейс
- Модели с валидацией

### 2. Telegram Bot (`telegram_bot/`)
**Назначение**: Интерфейс пользователя через Telegram

**Технологии**:
- Aiogram 3.4.1
- Aiogram-Dialog 2.1.0
- Асинхронный HTTP клиент

**Функциональность**:
- Диалоговые интерфейсы
- Создание и просмотр задач
- Интеграция с Django API
- Показ даты создания задач

### 3. Celery (`celery` сервис)
**Назначение**: Фоновые задачи и уведомления

**Функции**:
- Уведомления о сроках задач
- Периодическая проверка просроченных задач
- Отправка сообщений в Telegram

### 4. База данных (PostgreSQL)
**Назначение**: Хранение данных

**Структура**:
```sql
Categories:
- id (custom hash)
- name, description, color
- created_at

Tasks:
- id (custom hash)
- title, description
- status, priority
- category_id, user_id, telegram_user_id
- due_date, completed_at
- created_at, updated_at
```

### 5. Redis
**Назначение**: Кеш и очередь сообщений

**Использование**:
- Celery broker
- Состояния диалогов бота
- Кеширование API ответов

## Поток данных

### Создание задачи:
1. Пользователь взаимодействует с ботом
2. Бот собирает данные через диалоги
3. API клиент отправляет POST запрос в Django
4. Django создает задачу в PostgreSQL
5. Если есть срок - планируется Celery задача
6. Бот показывает подтверждение

### Уведомления:
1. Celery Beat проверяет просроченные задачи
2. Celery Worker отправляет уведомление
3. Бот получает запрос на отправку сообщения
4. Пользователь получает уведомление в Telegram

## Безопасность

- Все секреты в переменных окружения
- Валидация данных через DRF сериализаторы
- Изоляция сервисов в Docker контейнерах
- Отдельные пользователи в контейнерах

## Масштабируемость

- Горизонтальное масштабирование Celery workers
- Кеширование через Redis
- Статические файлы через CDN (в продакшене)
- Балансировка нагрузки Django (в продакшене)

## Мониторинг

- Логирование всех операций
- Health checks в Docker Compose
- Метрики Celery
- Django Admin для мониторинга данных