# 📋 ToDo List Bot

Telegram бот для управления задачами с Django backend и Celery уведомлениями.

> **Тестовое задание для бекенд-разработчика** - полнофункциональная система управления задачами с современным стеком технологий.

![Python](https://img.shields.io/badge/Python-3.11-blue)
![Django](https://img.shields.io/badge/Django-4.2.7-green)
![Aiogram](https://img.shields.io/badge/Aiogram-3.4.1-blue)
![Docker](https://img.shields.io/badge/Docker-Compose-blue)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15-blue)
![Redis](https://img.shields.io/badge/Redis-7-red)
![Celery](https://img.shields.io/badge/Celery-5.3.4-green)
![Status](https://img.shields.io/badge/Status-Production%20Ready-brightgreen)

## 🏗️ Архитектура решения

Проект состоит из следующих компонентов:

### Backend (Django)
- **Django REST API** - основной backend для управления задачами и категориями
- **PostgreSQL** - база данных для хранения задач, категорий и пользователей
- **Кастомные Primary Key** - используются хеши на основе timestamp вместо UUID или автоинкрементов
- **Административный интерфейс** - Django Admin для управления задачами
- **Часовой пояс America/Adak** - согласно требованиям задания

### Telegram Bot (Aiogram + Aiogram-Dialog)
- **Aiogram 3.x** - современная библиотека для Telegram ботов
- **Aiogram-Dialog** - диалоговая система для интерактивного взаимодействия
- **Интеграция с Django API** - все данные получаются через REST API
- **Показ даты создания** - в списке задач отображается дата создания

### Уведомления (Celery)
- **Celery Worker** - обработка фоновых задач
- **Celery Beat** - планировщик для периодических задач
- **Redis** - брокер сообщений для Celery
- **Уведомления о сроках** - автоматические напоминания при наступлении дедлайна

### Контейнеризация (Docker)
- **Docker Compose** - оркестрация всех сервисов
- **Отдельные контейнеры** - Django, PostgreSQL, Redis, Celery, Telegram Bot
- **Health checks** - проверка готовности сервисов
- **Volumes** - персистентное хранение данных

## 🚀 Запуск проекта

### Предварительные требования
- Docker и Docker Compose
- Telegram Bot Token (получить у @BotFather)

### Быстрый запуск

> 📖 **Для быстрого старта см. [QUICKSTART.md](QUICKSTART.md)**

**Автоматический запуск (рекомендуется):**

Linux/Mac:
```bash
chmod +x start_todo_project.sh
./start_todo_project.sh
```

Windows:
```cmd
start_todo_project.bat
```

**Ручной запуск:**

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd todo-bot
```

2. **Настройте переменные окружения**
```bash
cp .env.example .env
# Отредактируйте .env и укажите BOT_TOKEN от @BotFather
```

**Получение BOT_TOKEN:**
1. Найдите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в файл `.env`

3. **Запустите все сервисы**
```bash
docker-compose up -d
```

4. **Создайте миграции и начальные данные**
```bash
docker-compose exec django python manage.py makemigrations
docker-compose exec django python manage.py migrate
docker-compose exec django python manage.py create_initial_data
docker-compose exec django python manage.py createsuperuser
```

5. **Проверьте статус сервисов**
```bash
docker-compose ps
```

### Доступ к сервисам

- **Django Admin**: http://localhost:8000/admin/
- **Django API**: http://localhost:8000/api/
- **Telegram Bot**: найдите вашего бота в Telegram и отправьте /start

## 📱 Функциональность бота

### Основные возможности
- 📋 **Просмотр задач** - список всех задач с категориями и датами создания
- ➕ **Создание задач** - интерактивное создание через диалоги
- ✅ **Отметка выполнения** - быстрое завершение задач
- 🏷️ **Категории** - организация задач по категориям
- ⚡ **Приоритеты** - установка важности задач
- 📊 **Статистика** - общая статистика по задачам

### Диалоговое взаимодействие
Бот использует aiogram-dialog для создания интуитивных диалогов:

1. **Главное меню** - выбор основных действий
2. **Список задач** - просмотр и управление задачами
3. **Создание задачи** - пошаговое создание новой задачи
4. **Детали задачи** - подробная информация о выбранной задаче

## 🔧 API Endpoints

### Категории
- `GET /api/categories/` - список категорий
- `POST /api/categories/` - создание категории
- `GET /api/categories/{id}/` - детали категории
- `PUT/PATCH /api/categories/{id}/` - обновление категории
- `DELETE /api/categories/{id}/` - удаление категории

### Задачи
- `GET /api/tasks/` - список задач
- `POST /api/tasks/` - создание задачи
- `GET /api/tasks/{id}/` - детали задачи
- `PUT/PATCH /api/tasks/{id}/` - обновление задачи
- `DELETE /api/tasks/{id}/` - удаление задачи
- `POST /api/tasks/create_for_telegram/` - создание задачи для Telegram пользователя
- `GET /api/tasks/by_telegram_user/` - задачи по Telegram ID
- `PATCH /api/tasks/{id}/mark_completed/` - отметить как выполненную
- `GET /api/tasks/stats/` - статистика задач

## 🗂️ Структура проекта

```
todo-bot/
├── django_backend/          # Django приложение
│   ├── todo_project/       # Настройки проекта
│   │   ├── settings.py     # Конфигурация Django
│   │   ├── urls.py         # URL маршруты
│   │   ├── celery.py       # Настройки Celery
│   │   └── wsgi.py         # WSGI приложение
│   ├── todo_app/           # Основное приложение
│   │   ├── models.py       # Модели данных
│   │   ├── views.py        # API views
│   │   ├── serializers.py  # DRF сериализаторы
│   │   ├── admin.py        # Django Admin
│   │   ├── tasks.py        # Celery задачи
│   │   ├── urls.py         # URL маршруты приложения
│   │   └── management/     # Django команды
│   └── manage.py           # Django управление
├── telegram_bot/           # Telegram бот
│   ├── dialogs/           # Диалоги aiogram-dialog
│   │   ├── main_menu.py   # Главное меню
│   │   └── task_dialogs.py # Диалоги задач
│   ├── api_client.py      # Клиент для Django API
│   ├── config.py          # Конфигурация бота
│   ├── states.py          # Состояния диалогов
│   └── main.py            # Основной файл бота
├── docker-compose.yml      # Docker Compose конфигурация
├── Dockerfile.django       # Dockerfile для Django
├── Dockerfile.bot          # Dockerfile для бота
├── requirements.txt        # Python зависимости
├── .env.todo              # Пример переменных окружения
├── ARCHITECTURE.md         # Описание архитектуры
└── CHALLENGES.md          # Трудности и решения
```

## 🛠️ Трудности и решения

### 1. Кастомные Primary Key
**Проблема**: Требование не использовать UUID, random или автоинкременты для PK.

**Решение**: Создана функция `generate_custom_id()` которая генерирует 12-символьный хеш на основе текущего времени в микросекундах. Это обеспечивает уникальность и соответствует требованиям.

### 2. Интеграция Aiogram-Dialog с Django API
**Проблема**: Необходимость связать диалоговую систему бота с REST API.

**Решение**: Создан асинхронный API клиент `APIClient` который используется в диалогах для получения и отправки данных. Все операции выполняются через HTTP запросы к Django API.

### 3. Уведомления через Celery
**Проблема**: Настройка Celery для отправки уведомлений о задачах в Telegram.

**Решение**: Созданы Celery задачи в `todo_app/tasks.py` которые планируют и отправляют уведомления. Используется Celery Beat для периодической проверки просроченных задач.

### 4. Часовой пояс America/Adak
**Проблема**: Необходимость работы в специфическом часовом поясе.

**Решение**: В Django settings установлен `TIME_ZONE = 'America/Adak'` и `USE_TZ = True` для корректной работы с временными зонами.

### 5. Docker оркестрация
**Проблема**: Координация запуска множества сервисов с зависимостями.

**Решение**: Использованы health checks в docker-compose.yml и правильная последовательность запуска сервисов с условиями `depends_on`.

## 🧪 Тестирование

Подробные инструкции по тестированию см. в [TESTING.md](TESTING.md)

Быстрая проверка:
```bash
# Проверка API
curl http://localhost:8000/api/categories/
curl http://localhost:8000/api/tasks/stats/?telegram_user_id=123456789

# Статус сервисов
docker-compose ps
```

## 📝 Дополнительные возможности

- **Административный интерфейс** - полноценная админка Django для управления задачами
- **Фильтрация и поиск** - API поддерживает фильтрацию по статусу, категории, пользователю
- **Статистика** - подробная статистика по задачам пользователя
- **Логирование** - подробные логи всех операций
- **Масштабируемость** - архитектура позволяет легко добавлять новые функции

## 🔒 Безопасность

- Все пароли и токены в переменных окружения
- Валидация данных через DRF сериализаторы
- Изоляция сервисов в Docker контейнерах
- Отдельные пользователи в контейнерах

## 📄 Лицензия

MIT License